{
  "name": "sundai-marketing-site",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:full": "npx -y npm-run-all -p backend dev",
    "backend": "PYTHONPATH=.. uvicorn apps.src.app.server:app --reload --port 8000 --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --port 5173 --strictPort"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "vite": "^5.3.0"
  }
}


