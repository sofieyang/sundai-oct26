{
  "name": "sundai-oct26",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:full": "npx -y npm-run-all -p backend dev",
    "def:full": "npm run dev:full",
    "backend": "uvicorn app.server:app --reload --port 8000 --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "vite": "^5.3.0"
  }
}

